import React from 'react';
import useTitle from '../../Hooks/UserHooks';

const Blogs = () => {
    //title
    useTitle('Blogs')
    return (
        <div>

        </div>
    );
};

export default Blogs;